#include <cstring>
#include <iostream>

struct node{
  int data;
  char color;
  node* left;
  node* right;
};

class RBTree{
 public:
  RBTree();
  ~RBTree();
  //node* getParent(node* n);     //get parent of n
  //node* getSibling(node* n);
  //node* getUncle(node* n);
  int insert(int data);
  int insert(node* newnode, node* current);
  int contains(node* n);
  int visualize();
  int visualize(int data, node* cur);
 private:
  node* head;
};
